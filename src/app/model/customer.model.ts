export class Customer {
  constructor(public id: number, public clientNumber: string, public location: string, public email: string, public phoneNumber: string, public contactName: string) { }
}

